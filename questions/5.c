#include <stdio.h>

int main()
{
int length;
int breadth;
int area;

printf("Area of a rectangle:");
scanf("%d %d",&length,&breadth);
area = length * breadth;
printf("Area of a rectangle:%d\n",length*breadth);

return 0;

}

