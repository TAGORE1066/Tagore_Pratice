#include <stdio.h>
int main() {
    int a = 10;
    if (!(a > 15))
        printf("Condition is false, but NOT makes it true\n");
    else
        printf("Condition is true\n");
    return 0;
}
//when the condition is ture the o/p will be fale
//like it was change the o/p
